# FloodSense LK dependencies — pinned with ~= (compatible release)
# After installing: pip freeze > requirements.lock.txt for exact pins in production

# Agent framework
langgraph~=0.2
langchain-google-genai~=2.0

# API
fastapi~=0.115.0
uvicorn~=0.30.0
jinja2~=3.1.0
python-multipart~=0.0.9

# Database
asyncpg~=0.29.0

# Cache
redis[asyncio]~=5.0

# Scheduler
apscheduler~=3.10.0

# MCP client
mcp~=1.0

# Config & validation
pydantic~=2.0
pydantic-settings~=2.0

# HTTP client
httpx~=0.27.0

# Rate limiting
slowapi~=0.1.0

# Logging
structlog~=24.0

# Testing
pytest~=8.0
pytest-asyncio~=0.23.0
